name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  source-audit:
    name: Source Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: python3 scripts/audit_platform_guards.py
